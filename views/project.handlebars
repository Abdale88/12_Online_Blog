<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{description}}</p>
  <p>Written by {{user.name}} on {{format_date date_created}}</p>
 
  <p>
    <button class="btn btn-primary">Fund this Project!</button>
  </p>

  {{!-- here --}}
  <div class="row mt-4">
  <div class="col-md-6">
    <h3>Create a New Project:</h3>

    <form class="form update-project-form">
    <label for="title">
     Name:
    </label>
    <br />
    <textarea id="title" name="title" rows="2" cols="30">{{name}}</textarea>
    <br />
    <label for="title-desc">
      Description:
    </label>
    <br />
    <textarea id="title-desc" name="title-desc" rows="5" cols="30">{{description}}</textarea>
    <br />
    <label for="guest_name">
      Guest Name:
    </label>
    <br />
    <button type="submit" value="Submit">
      Submit
    </button>
  </form>

    {{!-- <form class="form update-project-form">
      <div class="form-group">
        <label for="title">project name:</label>
        <textarea class="form-input" type="text" id="title" name="title">{{project.name}}</textarea>
      </div>
      <div class="form-group">
        <label for="title-desc">description:</label>
        <textarea class="form-input" id="title-desc" name="title-desc">{{project.description}}</textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Create</button>
      </div>
    </form> --}}
  </div>
</div>

  {{!-- to here --}}
</div>
<script src="/js/updatedBlog.js"></script>


